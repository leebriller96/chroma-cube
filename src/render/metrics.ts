/**
 * 판을 짓는 치수. 발판과 큐브가 서로 아귀가 맞아야 해서 한곳에 모아 둔다.
 *
 * 발판은 칸 높이(1)를 거의 다 채운다. 그래야 층을 쌓았을 때 계단이 통짜 돌계단처럼 붙어 보이고,
 * 큐브가 한 층 오를 때 허공을 뛰는 대신 모서리를 타고 넘어가는 것으로 읽힌다.
 * 남는 틈은 돌과 돌 사이 이음매가 된다.
 */
export const SLAB_H = 0.88;
/** 발판 윗면이 칸의 y 다. 몸통은 그 아래로 SLAB_H 만큼 내려간다. */
export const SLAB_MID = -SLAB_H / 2;
export const CUBE_SIZE = 0.9;
